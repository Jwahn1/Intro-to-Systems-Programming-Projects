}
    return 0;
    }
        printf("%s", lines[i]);
    for (int i = n - 1; i >= 0; i--) {

    }
        n++
        lines[n] = strdup(tmp);
    while (fgets(tmp, 80, stdin) != NULL) {

    char *lines[100];
    int n = 0;
    char tmp[80];
int main() {

#include <string.h>
#include <stdio.h>
#include <stdio.h>
#include <string.h>

int main() {
    int n = 0;
    char tmp[80];
    char *lines[100];

    while (fgets(tmp, 80, stdin) != NULL) {
        lines[n] = strdup(tmp);
        n++
    }

    for (int i = n - 1; i >= 0; i--) {
        printf("%s", lines[i]);
    }
    return 0;
}
